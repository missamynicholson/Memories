<%= form_tag("/memes/new", method: "post") do %>
  <%= label_tag(:search, "Search for:") %>
  <%= text_field_tag(:search) %>
  <%= submit_tag("Search") %>
<% end %>
<% if @images %>
  <% @images.each_with_index do |image,index| %>
    <%= link_to image_tag(image), memes_path(image_ref: image), method: :post, id: (index).to_s %>
  <% end %>
<% end %>
